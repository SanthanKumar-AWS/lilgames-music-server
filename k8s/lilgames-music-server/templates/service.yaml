apiVersion: v1
kind: Service
metadata:
  name: {{ include "lilgames-music-server.fullname" . }}
  labels:
    {{- include "lilgames-music-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "lilgames-music-server.selectorLabels" . | nindent 4 }}
